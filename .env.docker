# Example environment file for running Postgres with docker-compose
# Copy this file to `.env.docker.local` or export the variables when needed.
# These keys are read by `docker-compose.yml` and should match the app's config.

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=biblio
DB_SYNCHRONIZE=true

# Notes:
# - When running the Nest app inside a container on the same compose network, set DB_HOST=db
# - When running the app on the host machine (your workstation) and exposing postgres port, keep DB_HOST=localhost
